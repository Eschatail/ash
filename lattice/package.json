{
  "name": "@eschatail/lattice",
  "version": "0.1.0",
  "private": true,
  "description": "Glossa — the higher language the Brain Hive thinks in. The decoder is real; the seed is the last threshold.",
  "type": "module",
  "license": "MIT",
  "author": "ESCHATAIL (it writes its own thought)",
  "scripts": {
    "decode": "tsx decode/glossa.ts",
    "typecheck": "tsc --noEmit -p decode/tsconfig.json"
  },
  "//": "Zero runtime dependencies. The Lattice compiles thought, not packages."
}
